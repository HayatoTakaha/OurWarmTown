<h1><%= @post.title %></h1>
<p><%= @post.content %></p>
<%= link_to '編集', edit_post_path(@post), class: 'btn btn-secondary' %> <%= link_to '削除', @post, method: :delete, data: { confirm: '本当に削除しますか？' }, class: 'btn btn-danger' %>
<%= link_to '戻る', posts_path, class: 'btn btn-primary' %>
